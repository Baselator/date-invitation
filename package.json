{
  "name": "date-invitation",
  "version": "1.0.0",
  "private": true,
  "description": "A static Netlify-ready date invitation app.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check netlify/functions/send-response.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
